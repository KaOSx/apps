commit 40f4b0700b5065bc9d90b49412322f77f3d58618
Author: Andrius Štikonas <andrius@stikonas.eu>
Date:   Thu Jun 5 02:08:02 2014 +0100

    Force the creation of btrfs filesystem in case the partition already contains filesystem.

diff --git a/src/fs/btrfs.cpp b/src/fs/btrfs.cpp
index f81f55d..5690278 100644
--- partitionmanager/src/fs/btrfs.cpp
+++ partitionmanager/src/fs/btrfs.cpp
@@ -128,7 +128,7 @@ namespace FS
 
 	bool btrfs::create(Report& report, const QString& deviceNode) const
 	{
-		ExternalCommand cmd(report, "mkfs.btrfs", QStringList() << deviceNode);
+		ExternalCommand cmd(report, "mkfs.btrfs", QStringList() << "-f" << deviceNode);
 		return cmd.run(-1) && cmd.exitCode() == 0;
 	}
 
