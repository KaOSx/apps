
pkgname=firefox
pkgdesc='Standalone web browser from mozilla.org'
url='http://www.mozilla.org/projects/firefox'
pkgver=30.0b8
pkgrel=1
arch=('x86_64')
license=('MPL' 'GPL' 'LGPL')
groups=('network')
depends=('desktop-file-utils' 'libxt' 'mime-types' 'nss' 'shared-mime-info' 'gtk2' 'networkmanager'
         'gst-plugins-good' 'gst-plugins-bad')
#source=("http://ftp.mozilla.org/pub/firefox/releases/${pkgver}/linux-x86_64/en-US/firefox-${pkgver}.tar.bz2"
source=("ftp://ftp.mozilla.org/pub/firefox/candidates/${pkgver}-candidates/build1/linux-x86_64/en-US/firefox-${pkgver}.tar.bz2"
        'firefox.desktop' 
        'firefox-safe.desktop')
md5sums=('8107b9454d7db04d2485f0b4913f106b'
         '4ea31ded5349d4f3992874e0eb87eb2a'
         '2f004e44dd2ef3138367ea5b1bc1a219')

package() {
  cd "${srcdir}"
  mkdir -p "${pkgdir}"/{usr/{bin,share/{applications,pixmaps}},opt}
  cp -r firefox "${pkgdir}/opt/firefox-${pkgver}"

  ln -s /opt/firefox-${pkgver}/firefox "${pkgdir}/usr/bin/firefox"
  install -m644 "${srcdir}"/{firefox.desktop,firefox-safe.desktop} "${pkgdir}/usr/share/applications/"
  install -m644 "${srcdir}/firefox/browser/icons/mozicon128.png" "${pkgdir}/usr/share/pixmaps/${pkgname}-icon.png"
}
