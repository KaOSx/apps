#
# Support Packages for Chakra, part of chakra-project.org
#
# maintainer (i686): Phil Miller <philm[at]chakra-project[dog]org>
# maintainer (x86_64): Manuel Tortosa <manutortosa[at]chakra-project[dot]org>

pkgname=msilbc
pkgver=2.0.1
pkgrel=1
pkgdesc="Low bitrate audio codec - plugin for mediastreamer"
url="http://www.linphone.org/"
arch=('i686' 'x86_64')
license=('GPL')
depends=('ilbc')
source=(http://download-mirror.savannah.gnu.org/releases/linphone/plugins/sources/$pkgname-$pkgver.tar.gz)
options=('!makeflags')
md5sums=('ec2855c57b4344f14fbbc8cfd4c433fe')

build() {
	cd "$srcdir/$pkgname-$pkgver"
	./configure --prefix=/usr
	make
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	make DESTDIR="$pkgdir" install
}
