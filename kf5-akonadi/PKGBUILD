
# include global config
source ../_buildscripts/${current_repo}-${_arch}-cfg.conf

pkgname=kf5-akonadi
_pkgname=akonadi
pkgver=5.0.40
pkgrel=2
arch=('x86_64')
url='http://community.kde.org/KDE_PIM/Akonadi'
pkgdesc="PIM layer, which provides an asynchronous API to access all kind of PIM data"
license=('LGPL')
depends=('shared-mime-info' 'boost-libs' 'sqlite' 'qt5-base' 'libxslt')
makedepends=('extra-cmake-modules' 'boost' 'postgresql' 'mariadb')
optdepends=('postgresql: PostgreSQL backend'
            'mariadb: MySQL backend')
install=akonadi.install
#source=("http://download.kde.org/stable/akonadi/src/akonadi-${pkgver}.tar.bz2")
source=("git://anongit.kde.org/akonadi")
md5sums=('SKIP')

build() {
  mkdir -p build
  cd build
  
  cmake ../${_pkgname} \
    -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DINSTALL_QSQLITE_IN_QT_PREFIX=TRUE \
    -DDATABASE_BACKEND=SQLITE \
    -DENABLE_ASAN=TRUE
  make
}

package() {
  cd build
  
  make DESTDIR="${pkgdir}" install
}
