
# include global config
source ../_buildscripts/${current_repo}-${_arch}-cfg.conf

pkgname=ktextaddons
pkgver=1.3.0
pkgrel=1
pkgdesc='Various text handling addons'
arch=('x86_64')
url='https://invent.kde.org/libraries/ktextaddons'
license=('LGPL')
depends=('ki18n' 'kconfigwidgets' 'karchive' 'kxmlgui' 'kcoreaddons' 'qtkeychain'
         'qt5-speech')
makedepends=('extra-cmake-modules' 'qt5-tools')
source=("https://download.kde.org/stable/ktextaddons/${pkgname}-${pkgver}.tar.xz")
sha256sums=('ce16a0e160cfe80e7a5c2cbb0b75f5c8f721fc6c5771d8b68bfdcf279fd6e1b7')

build() {
  cmake -B build -S ${pkgname}-${pkgver} \
    -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DKDE_INSTALL_LIBDIR=lib \
    -DBUILD_TESTING=OFF
  cmake --build build
}

package() {

  DESTDIR=${pkgdir} cmake --install build
}
