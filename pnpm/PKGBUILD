
pkgname=pnpm
pkgver=9.12.2
pkgrel=1
pkgdesc='Fast, disk space efficient package manager'
arch=('x86_64')
url="https://pnpm.io"
license=('MIT')
depends=('nodejs')
makedepends=('pnpm')
source=("https://github.com/pnpm/pnpm/archive/v${pkgver}/${pkgname}-${pkgver}.tar.gz")
sha256sums=('35d3a3c5343fcac0251ce9bfd3568e8b24ac097ca62832dcc7d6fa25bf7deeda')

package() {
  npm install -g \
    --cache ${srcdir}/npm-cache \
    --prefix ${pkgdir}/usr \
    pnpm

  cd ${pkgdir}/usr/lib/node_modules/pnpm
  rm -r dist/node-gyp-bin
  rm -r dist/vendor

  install -Dm644 LICENSE -t ${pkgdir}/usr/share/licenses/${pkgname}/
}
