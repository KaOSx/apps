
pkgname=accountsservice
pkgver=22.07.4
pkgrel=1
pkgdesc="Set of D-Bus interfaces for querying and manipulating user account information."
arch=('x86_64')
url="https://gitlab.freedesktop.org/accountsservice/accountsservice"
license=('GPL3')
depends=('dbus' 'glib2' 'polkit')
makedepends=('meson' 'ninja')
source=("https://www.freedesktop.org/software/accountsservice/${pkgname}-${pkgver}.tar.xz")
sha256sums=('abcc3039506c8d59f874957c78b252e6d7e52ba78c0f44b8aa4f40988d5fe962')

build() {
  mkdir -p build
  cd build

  meson ../${pkgname}-${pkgver} \
    --prefix=/usr \
    --buildtype=release \
    --libexecdir=lib \
    -Dadmin_group=wheel \
    -Dintrospection=false
  ninja
}

package() {
  cd build

  DESTDIR=${pkgdir} ninja install
}

