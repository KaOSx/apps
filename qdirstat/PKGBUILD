
pkgname=qdirstat
arch=('x86_64')
pkgver=1.5
_pkgver=1.5
pkgrel=1
pkgdesc="Qt-based directory statistics: KDirStat without any KDE -- from the author of the original KDirStat."
url="https://github.com/shundhammer/qdirstat"
license=('GPL')
depends=('qt5-base')
makedepends=('qt5-tools')
source=("https://github.com/shundhammer/qdirstat/archive/${_pkgver}.tar.gz")
md5sums=('fa820040430b92f8b8efca21bd7bb07d')

build() {  
  cd ${pkgname}-${_pkgver}

  /usr/lib/qt5/bin/qmake
  make
}

package() {
  cd ${pkgname}-${_pkgver}

  make INSTALL_ROOT=${pkgdir} install
}
