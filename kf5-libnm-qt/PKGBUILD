
pkgname=kf5-libnm-qt
_pkgname=libnm-qt
pkgver=5.0.93
_pkgver=5.0.2
pkgrel=1
pkgdesc='Qt-only wrapper for NetworkManager DBus API'
arch=('x86_64')
url='https://projects.kde.org/projects/extragear/libs/libnm-qt'
license=('LGPL')
depends=('qt5-base' 'networkmanager')
makedepends=('extra-cmake-modules')
source=("http://download.kde.org/stable/plasma/$_pkgver/$_pkgname-$pkgver.tar.xz")
md5sums=('0079a9adc811ffbaacb9731b9e3646e4')
#source=("git://anongit.kde.org/libnm-qt.git")
#md5sums=('SKIP')

build() {
  cd $_pkgname-$pkgver
  mkdir build
  
  cd build
  cmake .. \
    -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DLIB_INSTALL_DIR=lib \
    -DINCLUDE_INSTALL_DIR=/usr/include/KF5
  make
}

package() {
  cd $_pkgname-$pkgver/build
  
  make DESTDIR=${pkgdir} install
}
