post_install() {
  if [ ! -f /home/*/.config/plasmarc ]; then
    cp /etc/xdg/kdeglobals /home/*/.config
    cp /etc/xdg/ksplashrc /home/*/.config
    cp /etc/xdg/plasma-org.kde.plasma.desktop-appletsrc /home/*/.config
    cp /etc/xdg/plasmarc /home/*/.config
    
    /usr/bin/welcome
  fi
}
