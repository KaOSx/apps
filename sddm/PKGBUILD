
pkgname=sddm
pkgver=20140512
pkgrel=1
pkgdesc="QML based X11 display manager"
arch=('x86_64')
url="http://github.com/sddm/sddm"
license=('GPL')
depends=('qt5-declarative' 'libxcb')
makedepends=('cmake' 'git' 'qt5-tools')
conflicts=('sddm-qt5-git')
provides=('sddm-qt5-git')
replaces=('sddm-qt5-git')
backup=('etc/sddm.conf')
source=('git://github.com/blue-shell/sddm.git'
        'background.png')
md5sums=('SKIP'
         '4932058967e1fe4df3e8cd75b75997ca')

_gitroot="github.com/blue-shell/sddm.git"
_gitname=sddm

build() {
  cd "$srcdir"

  mkdir -p $srcdir/build
  cd build

  cmake ../${_gitname} -DCMAKE_INSTALL_PREFIX=/usr -DUSE_QT5=true
  make
}

package() {
  cd ${srcdir}/build
  make DESTDIR="${pkgdir}" install
  sed -i -e "s|white|deepskyblue|g" $pkgdir/usr/share/apps/sddm/themes/maui/Main.qml
  
  rm "${pkgdir}"/usr/share/apps/sddm/themes/maui/background.png
  install -m 644 -p ${srcdir}/background.png ${pkgdir}/usr/share/apps/sddm/themes/maui/  
}
