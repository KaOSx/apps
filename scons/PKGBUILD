
pkgname=scons
_pkgname=SCons
pkgver=4.6.0
_pkgver=4.6.0
pkgrel=1
pkgdesc="Extensible Python-based build utility"
arch=('x86_64')
url="https://scons.org"
license=('MIT')
depends=('python3')
makedepends=('python3-build' 'python3-installer' 'python3-setuptools' 'python3-wheel')
groups=('programming')
source=("https://pypi.io/packages/source/S/SCons/${_pkgname}-${_pkgver}.tar.gz")
sha256sums=('7db28958b188b800f803c287d0680cc3ac7c422ed0b1cf9895042c52567803ec')

build() {
  cd ${_pkgname}-${pkgver}

  python3 -m build -nw
}

package() {
  cd ${_pkgname}-${_pkgver}

  python3 -m installer --destdir=${pkgdir} dist/*.whl
  install -D -m644 README.rst ${pkgdir}/usr/share/licenses/${pkgname}/README
}

