
pkgname=ring-kde
pkgver=2.1.0
pkgrel=2
pkgdesc="KDE client for the Ring communication framework"
arch=('x86_64')
url='http://ring.cx/'
license=('LGPL')
depends=('kconfig' 'kguiaddons' 'kdbusaddons' 'ki18n' 'kinit' 'kwindowsystem'
         'kxmlgui' 'knotifications' 'kiconthemes' 'kio' 'kcompletion' 'kcrash'
         'qt5-svg' 'akonadi' 'ring-lrc')
makedepends=('extra-cmake-modules' 'kdoctools' 'git')
source=("git://anongit.kde.org/ring-kde.git#branch=profile"
        'qt5.5.diff')
md5sums=('SKIP'
         '603f6d30dfc2742ae573959a2b519b1b')

prepare() {
  cd $pkgname
  #patch -p1 -i $srcdir/qt5.5.diff
}

build() {
  mkdir -p build
  cd build
  
  cmake ../${pkgname} \
    -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DSYSCONF_INSTALL_DIR=/etc \
    -DPLUGIN_INSTALL_DIR=/usr/lib/qt5/plugins \
    -DKDE_INSTALL_USE_QT_SYS_PATHS=ON \
    -DLIB_INSTALL_DIR=lib
  make
}

package() {
  cd build

  make DESTDIR=${pkgdir} install
}
