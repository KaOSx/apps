
pkgname=kf5-ktp-desktop-applets
_pkgname=ktp-desktop-applets
pkgver=0.9.90
pkgrel=1
pkgdesc="Plasma applet for managing your Telepathy account presence"
arch=('x86_64')
url="https://projects.kde.org/projects/extragear/network/telepathy/ktp-desktop-applets"
license=('GPL')
depends=('kf5-ktp-common-internals' 'kf5-ktp-accounts-kcm' 'plasma-workspace')
makedepends=('extra-cmake-modules' 'kdoctools' 'git')
#groups=('kde-telepathy' 'utils' 'communication')
conflicts=('ktp-desktop-applets')
source=("git://anongit.kde.org/ktp-desktop-applets.git#branch=frameworks")
md5sums=('SKIP')

build() {
  cd ${srcdir}
  
  mkdir -p build
  cd build

  cmake ../${_pkgname} \
    -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DLIB_INSTALL_DIR=lib \
    -DSYSCONF_INSTALL_DIR=/etc \
    -DQML_INSTALL_DIR=/usr/lib/qt5/qml \
    -DKDE_INSTALL_USE_QT_SYS_PATHS=ON \
    -DBUILD_TESTING=OFF 
  make
}

package() {
  cd build

  make DESTDIR=${pkgdir} install
}

